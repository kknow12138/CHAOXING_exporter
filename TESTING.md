# 测试要求

每次修改浏览器扩展后，必须完成两类验证，才算真正完成修改。

## 1. 本地回归

运行：

```bash
npm test
```

该命令会检查扩展脚本语法，并重新构建 `background/service-worker.js`。

## 2. 真实网站抓取

在用户指定的学习通作业页面验证抓取效果。

测试前需要确认：

- `TEST_TARGET_URL`：指定的学习通作业/考试/测验页面地址
- 浏览器已经登录可访问该页面
- 扩展已在浏览器扩展管理页重新加载

测试步骤：

1. 打开 `TEST_TARGET_URL`。
2. 点击扩展图标，选择“学习通”导出。
3. 等待扩展显示抓取成功并下载 Word。
4. 打开导出的 Word，核对题目数量、题干、选项、答案、解析。
5. 若本次修改涉及图片题或图片答案，必须确认图片已出现在 Word 中；若图片加载失败，需要记录失败链接或错误现象。

反馈格式：

```text
本地回归：通过/失败
真实网站：通过/失败/未执行
测试页面：<URL 或页面名称>
抓取题数：<数量>
导出文件：<文件名>
图片题/图片答案：通过/失败/不适用
备注：<异常、限制或需要用户协助的事项>
```

如果没有指定 `TEST_TARGET_URL`、缺少登录态、页面不可访问，真实网站测试必须标记为“未执行”，不能声称完成真实抓取验证。
